/*
Triangle::Triangle(float shininess, const mat4& transform, const vec3& v1, const vec3& v2, const vec3& v3)
    : shininess(shininess), transform(transform), v1(v1), v2(v2), v3(v3) {
}

Sphere::Sphere(float shininess, const mat4& transform, const vec3& center, float radius)
    : shininess(shininess), transform(transform), center(center), radius(radius) {
}
*/